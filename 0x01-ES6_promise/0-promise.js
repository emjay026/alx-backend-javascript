export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Simulate an asynchronous operation, for example, fetching data from an API
    setTimeout(() => {}, 1000); // Simulate a delay of 1 second
  });
}
