// Define a function named displayMessage that takes a single argument 'message'
function displayMessage(message) {
  // Print the message to the standard output using console.log
  console.log(message);
}

// Export the displayMessage function so that it can be used in other scripts
module.exports = displayMessage;
